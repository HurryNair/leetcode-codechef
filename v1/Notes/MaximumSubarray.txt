- O(n) solution using Kadanes algorithm
- Init var max_so_far to negative infinty
- Init max_ending_here to 0
- Iterate thru the array, add numbers to max_ending_here
- If max_ending_here > max_so_far : max_so_far = max_ending_here
- If max_ending_here < 0 : max_ending_here = 0